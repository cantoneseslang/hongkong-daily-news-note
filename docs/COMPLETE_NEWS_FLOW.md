# 香港デイリーニュース生成システム - 完全フロー

## 📋 目次
1. [システム概要](#システム概要)
2. [ニュース取得フェーズ](#ニュース取得フェーズ)
3. [フィルタリング・重複除外フェーズ](#フィルタリング重複除外フェーズ)
4. [カテゴリー分類フェーズ](#カテゴリー分類フェーズ)
5. [バランス選択フェーズ](#バランス選択フェーズ)
6. [記事生成フェーズ](#記事生成フェーズ)
7. [自動化フロー](#自動化フロー)
8. [設定・パラメータ](#設定パラメータ)

---

## システム概要

香港のニュースを自動収集し、日本語のMarkdown記事として生成・投稿するシステムです。

### 主な特徴
- **多ソース取得**: Webスクレイピング + RSSフィード
- **24時間以内フィルタリング**: 最新ニュースのみを取得
- **重複除外**: URL・タイトル・トピックの3段階チェック
- **カテゴリー分類**: 自動カテゴリー分類とバランス選択
- **LLM翻訳**: Grok/Gemini/Claude APIで日本語記事生成
- **自動投稿**: GitHub Actions + note.com自動投稿

---

## ニュース取得フェーズ

### Phase 1-1: Webスクレイピング (`scrape_news_list.py`)

#### 実装方法
Playwrightを使用してJavaScriptで動的生成されるページから記事を取得します。

#### 取得ソース

**1. Google News HK**
- **URL**: `https://news.google.com/topics/CAAqJQgKIh9DQkFTRVFvSUwyMHZNRE5vTmpRU0JYcG9MVWhMS0FBUAE?hl=zh-HK&gl=HK&ceid=HK:zh-Hant`
- **方法**: ページをスクロールして全記事を読み込み
- **取得数**: 約207件（24時間以内で約106件）
- **実装詳細**:
  ```python
  # ページを5回スクロールして全記事を読み込む
  for i in range(5):
      page.evaluate("window.scrollTo(0, document.body.scrollHeight)")
      page.wait_for_timeout(2000)
  
  # articleタグから記事を取得
  articles = page.query_selector_all('article')
  
  # テキストからタイトルと時刻を抽出
  # パターン: ソース名\n更多\nタイトル\n時刻
  ```

**2. HK01（香港01）**
- **URL**: 
  - `https://www.hk01.com/`
  - `https://www.hk01.com/zone/1/%E6%B8%AF%E8%81%9E` (港聞)
  - `https://www.hk01.com/channel/310/%E6%94%BF%E6%83%85` (政情)
  - `https://www.hk01.com/channel/4/%E7%B6%93%E6%BF%9F` (經濟)
- **方法**: Next.jsのSPAからJSONデータを抽出
- **取得数**: 約47件
- **実装詳細**:
  ```python
  # __NEXT_DATA__スクリプトタグからJSONを取得
  json_script = page.query_selector('script#__NEXT_DATA__')
  data = json.loads(json_text)
  
  # props.initialProps.pageProps.sections から記事を再帰的に抽出
  def extract_articles_from_data(data_obj, articles_list):
      # articleId と canonicalUrl を持つオブジェクトを探す
  ```

**3. 明報（Ming Pao）**
- **URL**: `https://news.mingpao.com/pns/港聞`
- **状態**: ネットワークエラーで取得不可（将来対応）

**4. am730**
- **URL**: `https://www.am730.com.hk/news`
- **状態**: ネットワークエラーで取得不可（将来対応）

#### 記事本文取得（オプション）
- `--content` フラグで有効化
- 各記事のURLにアクセスして本文の最初の500文字を取得
- `--translate` フラグで日本語翻訳も可能

### Phase 1-2: RSSフィード取得 (`fetch_rss_news.py`)

#### RSSソース一覧

| ソース | URL | 取得数 |
|--------|-----|--------|
| SCMP Hong Kong | `https://www.scmp.com/rss/2/feed` | 約10件 |
| SCMP Business | `https://www.scmp.com/rss/5/feed` | 約5件 |
| SCMP Lifestyle | `https://www.scmp.com/rss/322184/feed` | 約2件 |
| RTHK News | `https://rthk.hk/rthk/news/rss/e_expressnews_elocal.xml` | 約5件 |
| RTHK Business | `https://rthk.hk/rthk/news/rss/e_expressnews_ebusiness.xml` | 約0件 |
| Yahoo News HK | `http://hk.news.yahoo.com/rss/hong-kong` | 約0件（フィルタ済み） |
| Google News HK RSS | `https://news.google.com/rss/topics/...` | 約33件 |
| China Daily HK | `http://www.chinadaily.com.cn/rss/hk_rss.xml` | 約0件（フィルタ済み） |
| Hong Kong Free Press | `https://www.hongkongfp.com/feed/` | 約0件（フィルタ済み） |
| HKET Hong Kong | `https://www.hket.com/rss/hongkong` | 約2件 |
| HKET Finance | `https://www.hket.com/rss/finance` | 約4件 |
| HKET Property | `https://www.hket.com/rss/property` | 約0件 |

#### RSS取得処理
```python
# feedparserでRSSをパース
feed = feedparser.parse(rss_url)

# 各エントリーを処理
for entry in feed.entries[:100]:  # 最大100件
    url = entry.get('link', '')
    title = entry.get('title', '')
    description = entry.get('summary', '')
    published_at = entry.get('published', '')
```

### Phase 1-3: 記事全文取得 (`scrape_article.py`)

各記事のURLにアクセスして全文を取得します。

```python
# サイト別のセレクターを使用
if 'thestandard.com.hk' in url:
    content = self._scrape_standard(soup)
elif 'scmp.com' in url:
    content = self._scrape_scmp(soup)
else:
    content = self._scrape_generic(soup)
```

**保存形式**: `rss_news_YYYY-MM-DD_HH-MM-SS.json`
```json
{
  "fetch_time": "2025-11-20T12:00:00+08:00",
  "total_count": 150,
  "news": [
    {
      "title": "記事タイトル",
      "description": "記事の説明",
      "full_content": "記事全文",
      "url": "https://...",
      "published_at": "2025-11-20T10:00:00+08:00",
      "source": "SCMP",
      "api_source": "rss_scmp_hongkong"
    }
  ],
  "weather": {...}
}
```

---

## フィルタリング・重複除外フェーズ

### 1. 24時間以内フィルタリング

```python
def _is_today_news(self, published_at: str) -> bool:
    """過去24時間以内のニュースのみを含める"""
    pub_date = date_parser.parse(published_at)
    now = datetime.now(HKT)
    time_diff = now - pub_date.replace(tzinfo=None)
    return time_diff.total_seconds() <= 24 * 3600
```

**適用箇所**:
- RSSフィード取得時
- スクレイピング結果統合時
- Google News HKスクレイピング時（時刻解析で追加チェック）

### 2. 香港関連度チェック

```python
def _is_hk_related(self, title: str, description: str, url: str, source: str) -> bool:
    """香港関連記事かチェック"""
    
    # ホワイトリスト（必ず含める）
    whitelist = ['BN(O)', 'SCMP', 'RTHK', 'HKFP', '香港', 'Hong Kong', 'HK ']
    
    # ブラックリスト（除外）
    blacklist = [
        'Ukraine', 'Russia', 'Netherlands', 'Turkey', 
        '日本', '愛知', '東京', '大阪', 'Israel', 'Palestine'
    ]
    
    # ソース別の厳格度調整
    if source == 'Google News HK':
        # より厳格なチェック
        if not any(kw in title.lower() for kw in ['hong kong', 'hongkong', '香港', 'hk ']):
            return False
```

### 3. 禁止コンテンツフィルタリング

```python
def _is_forbidden_content(self, title: str, description: str) -> bool:
    """禁止コンテンツかチェック"""
    
    forbidden_keywords = [
        '全国運動会', '全運會', '全运会', 'national games',  # NGワード
        'wwii', 'ww2', '二次大戦',  # 歴史関連
        '競馬', '賽馬', '賭博',  # ギャンブル
        '47人', 'democracy trial',  # 政治関連
        'presented', 'sponsored', 'advertorial'  # 広告記事
    ]
    
    text = (title + ' ' + description).lower()
    return any(keyword in text for keyword in forbidden_keywords)
```

### 4. URL重複チェック

```python
def _normalize_url(self, url: str) -> str:
    """URLを正規化して重複チェック"""
    # クエリパラメータを除去
    url = url.split('?')[0].split('#')[0]
    # 末尾のスラッシュを除去
    url = url.rstrip('/')
    return url.lower()

# 処理済みURL履歴を管理
processed_urls = {
    "normalized_url": "2025-11-20T10:00:00+08:00"
}
```

### 5. タイトル重複チェック

```python
def _is_duplicate_content(self, title: str, existing_titles: List[str]) -> bool:
    """タイトルの類似度をチェック"""
    # 正規化して比較
    normalized = normalize_title(title)
    for existing in existing_titles:
        if normalized == normalize_title(existing):
            return True
    return False
```

---

## カテゴリー分類フェーズ

### カテゴリー分類ロジック (`generate_article.py`)

```python
def categorize_news(news: Dict) -> str:
    """ニュースをカテゴリーに分類"""
    
    title = news.get('title', '')
    description = news.get('description', '')
    content = title + ' ' + description
    
    # キーワードベースの分類
    if any(kw in content for kw in ['選挙', 'election', '立法会', 'legislative']):
        return '政治・行政'
    elif any(kw in content for kw in ['株', 'stock', '経済', 'economy', '金融']):
        return 'ビジネス・経済'
    elif any(kw in content for kw in ['スポーツ', 'sports', '試合', '選手']):
        return 'スポーツ'
    elif any(kw in content for kw in ['MTR', '交通', 'transport', 'バス', 'bus']):
        return '交通'
    elif any(kw in content for kw in ['火災', 'fire', '事故', 'accident']):
        return '事故・災害'
    elif any(kw in content for kw in ['逮捕', 'arrest', '犯罪', 'crime']):
        return '治安・犯罪'
    elif any(kw in content for kw in ['AI', 'テクノロジー', 'technology', 'IT']):
        return 'テクノロジー'
    elif any(kw in content for kw in ['文化', 'culture', '映画', 'movie', '音楽']):
        return 'カルチャー'
    else:
        return '社会・その他'
```

### カテゴリー一覧

| カテゴリー | 説明 | 制限件数 |
|-----------|------|---------|
| ビジネス・経済 | 経済、金融、株価、企業ニュース | 20件 |
| 社会・その他 | 一般的な社会ニュース | 15件 |
| カルチャー | 文化、エンタメ、ライフスタイル | 12件 |
| 政治・行政 | 政治、選挙、行政 | 12件 |
| テクノロジー | IT、AI、テクノロジー | 10件 |
| 交通 | MTR、バス、交通機関 | 8件 |
| 不動産 | 不動産、住宅 | 8件 |
| 事故・災害 | 火災、事故、災害 | 5件 |
| 治安・犯罪 | 逮捕、犯罪、治安 | 5件 |
| 医療・健康 | 医療、健康 | 5件 |
| 教育 | 教育関連 | 3件 |
| スポーツ | スポーツ（全運会除外） | 3件 |

**合計**: 100件

---

## バランス選択フェーズ

### 選択ロジック (`generate_article.py`)

```python
def preprocess_news(news_list):
    """ニュースを事前処理して選択"""
    
    # 1. 重複除外
    unique_news = remove_duplicates(news_list)
    
    # 2. カテゴリー分類
    categorized = {}
    for news in unique_news:
        category = categorize_news(news)
        categorized[category].append(news)
    
    # 3. バランス選択（3段階）
    selected = []
    target_count = 100
    
    # 1st pass: カテゴリー制限とソース制限を厳守
    select_news(limit_source=True, enforce_category_limit=True)
    
    # 2nd pass: ソース制限を緩和、カテゴリー制限は維持
    if len(selected) < target_count:
        select_news(limit_source=False, enforce_category_limit=True)
    
    # 3rd pass: カテゴリー制限も緩和
    if len(selected) < target_count:
        select_news(limit_source=False, enforce_category_limit=False)
    
    return selected
```

### 選択基準

1. **カテゴリー制限**: 各カテゴリーの上限を守る
2. **ソース制限**: 1ソースあたり最大4件（初期）
3. **トピック重複回避**: 過去3日間の記事とトピックが重複しない
4. **タイトル類似度チェック**: 類似したタイトルを除外

### トピック重複チェック

```python
def is_overused_topic(news: Dict, recent_topics: Dict[str, int]) -> bool:
    """過去3日間で同じトピックが2回以上出現しているかチェック"""
    
    # タイトルから主要トピックを抽出
    topic_keywords = ['全国運動会', '立法会選挙', 'MTR', ...]
    
    for keyword in topic_keywords:
        if keyword in news['title']:
            count = recent_topics.get(keyword, 0)
            if count >= 2:  # 2回以上出現
                return True
    
    return False
```

---

## 記事生成フェーズ

### LLM記事生成 (`generate_article.py`)

#### プロンプト構築

```python
system_prompt = """あなたは香港のニュースを日本語に翻訳し、記事を生成する専門家です。

翻訳ルール：
- すべてのテキストを自然な日本語に翻訳
- 香港の地名、人名、組織名は適切に翻訳
- ニュースの内容を正確に伝える
- 読みやすい記事形式で構成

記事構成：
- 各ニュースを### 見出しで区切る
- 内容を詳しく翻訳
- 引用元、リンク、備考を適切に配置

引用情報の形式（重要）：
各ニュースの最後に必ず以下の形式で記載してください：

**引用元**: ソース名
**リンク**: 完全なURL
"""

user_prompt = f"""
以下の香港のニュースを日本語のMarkdown記事に翻訳してください。

日付: {today_date}

ニュース:
{news_text}
"""
```

#### LLM API呼び出し

```python
# Grok API使用時
response = requests.post(
    'https://api.x.ai/v1/chat/completions',
    headers={'Authorization': f'Bearer {api_key}'},
    json={
        'model': 'grok-3',
        'messages': [
            {'role': 'system', 'content': system_prompt},
            {'role': 'user', 'content': user_prompt}
        ],
        'max_tokens': 16000,
        'temperature': 0.7
    }
)
```

#### 記事形式

```markdown
# 香港デイリーニュース 2025年11月20日

## 天気情報

### 今日の天気予報

香港天文台が2025年11月20日05時45分に発表した天気報告...

---

## ニュース

### 記事タイトル1

記事の内容を詳しく翻訳...

**引用元**: SCMP
**リンク**: https://www.scmp.com/...

---

### 記事タイトル2

記事の内容を詳しく翻訳...

**引用元**: RTHK
**リンク**: https://rthk.hk/...

---

## 広東語学習コーナー

今日の広東語フレーズ...
```

### 天気情報統合

```python
# fetch_rss_news.pyで取得
weather_data = {
    'forecast': '...',
    'warning': '...',
    'current': '...'
}

# generate_article.pyで統合
article_content = f"""
# 香港デイリーニュース {date}

## 天気情報

{format_weather_info(weather_data)}

## ニュース

{translated_news_content}
"""
```

### 保存

**ファイル名**: `hongkong-news_YYYY-MM-DD.md`
**保存場所**: `daily-articles/`

---

## 自動化フロー

### GitHub Actionsワークフロー (`.github/workflows/daily-news.yml`)

```yaml
name: Daily Hong Kong News Generator

on:
  schedule:
    - cron: '0 22 * * *'  # UTC 22:00 = HKT 06:00
  workflow_dispatch:  # 手動実行も可能

jobs:
  generate-daily-news:
    steps:
      # 1. リポジトリをチェックアウト
      - uses: actions/checkout@v4
      
      # 2. Python環境セットアップ
      - uses: actions/setup-python@v4
      
      # 3. 依存関係インストール
      - run: pip install -r requirements.txt
      
      # 4. 設定ファイル作成（シークレットから）
      - run: |
          cat > config.json << EOF
          {
            "grok_api": {"api_key": "${{ secrets.GROK_API_KEY }}"},
            ...
          }
          EOF
      
      # 5. RSSニュース取得
      - run: python fetch_rss_news.py
      
      # 6. 記事生成
      - run: |
          LATEST_RSS=$(ls -t daily-articles/rss_news_*.json | head -1)
          python generate_article.py "$LATEST_RSS"
      
      # 7. 記事検証
      - run: python scripts/sanitize_article.py "daily-articles/hongkong-news_*.md"
      
      # 8. GitHubにコミット・プッシュ
      - run: |
          git add daily-articles/hongkong-news_*.md
          git commit -m "📰 Daily news: $(date +%Y-%m-%d) [automated]"
          git push origin main
      
      # 9. note.comに投稿
      - run: |
          node note_auto_post.js "daily-articles/hongkong-news_*.md" \
            "${{ secrets.NOTE_EMAIL }}" \
            "${{ secrets.NOTE_PASSWORD }}" \
            --publish
```

### 実行タイミング

- **自動実行**: 毎日HKT 06:00（UTC 22:00）
- **手動実行**: GitHub ActionsのUIから「Run workflow」で実行可能

---

## 設定・パラメータ

### 記事選択パラメータ (`generate_article.py`)

```python
target_count = 100  # 目標記事数
max_per_source_initial = 4  # 1ソースあたりの最大件数（初期）
fallback_category_limit = 10  # カテゴリー未指定時の制限

category_limits = {
    'ビジネス・経済': 20,
    '社会・その他': 15,
    'カルチャー': 12,
    '政治・行政': 12,
    'テクノロジー': 10,
    '交通': 8,
    '不動産': 8,
    '事故・災害': 5,
    '治安・犯罪': 5,
    '医療・健康': 5,
    '教育': 3,
    'スポーツ': 3,
}
```

### フィルタリングパラメータ (`fetch_rss_news.py`)

```python
# 24時間以内フィルタリング
TIME_LIMIT_HOURS = 24

# RSS取得制限
RSS_FEED_LIMIT = 100  # 各RSSフィードから最大100件

# 処理済みURL履歴
PROCESSED_URLS_FILE = 'daily-articles/processed_urls.json'
URL_HISTORY_DAYS = 7  # 7日間の履歴を保持
```

### LLM設定 (`config.json`)

```json
{
  "grok_api": {
    "api_key": "...",
    "api_url": "https://api.x.ai/v1/chat/completions",
    "model": "grok-3"
  },
  "gemini_api": {
    "api_key": "...",
    "api_url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent"
  },
  "claude_api": {
    "api_key": "...",
    "api_url": "https://api.anthropic.com/v1/messages"
  }
}
```

---

## データフロー図

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 1: ニュース取得                                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐      ┌──────────────┐                    │
│  │ Webスクレイピング│      │ RSSフィード  │                    │
│  │              │      │              │                    │
│  │ Google News  │      │ SCMP         │                    │
│  │ HK01         │      │ RTHK         │                    │
│  │ 明報・am730   │      │ Yahoo HK     │                    │
│  │              │      │ Google News  │                    │
│  └──────┬───────┘      └──────┬───────┘                    │
│         │                      │                            │
│         └──────────┬───────────┘                            │
│                    │                                        │
│                    ▼                                        │
│         ┌─────────────────────┐                             │
│         │ 統合・重複チェック   │                             │
│         └──────────┬──────────┘                             │
│                    │                                        │
│                    ▼                                        │
│         ┌─────────────────────┐                             │
│         │ 24時間以内フィルタ   │                             │
│         └──────────┬──────────┘                             │
│                    │                                        │
│                    ▼                                        │
│         ┌─────────────────────┐                             │
│         │ 香港関連度チェック   │                             │
│         └──────────┬──────────┘                             │
│                    │                                        │
│                    ▼                                        │
│         ┌─────────────────────┐                             │
│         │ 禁止コンテンツ除外   │                             │
│         └──────────┬──────────┘                             │
│                    │                                        │
│                    ▼                                        │
│         ┌─────────────────────┐                             │
│         │ 記事全文取得         │                             │
│         └──────────┬──────────┘                             │
│                    │                                        │
│                    ▼                                        │
│    rss_news_YYYY-MM-DD_HH-MM-SS.json                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Phase 2: 記事生成                                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  rss_news_*.json                                            │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────────────┐                                    │
│  │ ニュースデータ読み込み │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                    │
│  │ 重複除外             │                                    │
│  │ - URL重複            │                                    │
│  │ - タイトル類似度      │                                    │
│  │ - トピック重複        │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                    │
│  │ カテゴリー分類       │                                    │
│  │ - ビジネス・経済     │                                    │
│  │ - 政治・行政         │                                    │
│  │ - 社会・その他       │                                    │
│  │ - テクノロジー       │                                    │
│  │ - カルチャー         │                                    │
│  │ - その他             │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                    │
│  │ バランス選択         │                                    │
│  │ - カテゴリー制限     │                                    │
│  │ - ソース制限         │                                    │
│  │ - 目標: 100件        │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                    │
│  │ LLM記事生成          │                                    │
│  │ - 日本語翻訳         │                                    │
│  │ - Markdown形式       │                                    │
│  │ - 引用元・リンク追加  │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│             ▼                                               │
│  hongkong-news_YYYY-MM-DD.md                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Phase 3: 投稿                                               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  hongkong-news_*.md                                         │
│         │                                                    │
│         ├──────────────────┬─────────────────┐             │
│         │                  │                 │             │
│         ▼                  ▼                 ▼             │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐         │
│  │ GitHub   │      │ note.com │      │ ローカル  │         │
│  │ コミット  │      │ 自動投稿  │      │ 保存      │         │
│  └──────────┘      └──────────┘      └──────────┘         │
└─────────────────────────────────────────────────────────────┘
```

---

## 期待される結果

### 取得記事数
- **スクレイピング**: 約153件（Google News HK: 106件 + HK01: 47件）
- **RSSフィード**: 約50件
- **合計**: 約200件

### フィルタリング後
- **24時間以内**: 約150件
- **香港関連**: 約120件
- **重複除外後**: 約100件

### 最終記事
- **選択記事数**: 100件（バランス選択）
- **カテゴリー別**: バランス良く配分
- **形式**: Markdown形式の日本語記事
- **投稿先**: GitHub + note.com

---

## トラブルシューティング

### よくある問題

1. **記事数が少ない**
   - RSSフィードの取得状況を確認
   - フィルタリング条件を緩和
   - スクレイピングのエラーを確認

2. **重複記事が多い**
   - URL正規化ロジックを確認
   - 処理済みURL履歴をクリア

3. **カテゴリーが偏っている**
   - `category_limits`を調整
   - カテゴリー分類ロジックを改善

4. **LLM生成エラー**
   - APIキーを確認
   - トークン制限を確認
   - プロンプトを調整

---

## 今後の改善予定

1. **スクレイピングソース追加**
   - 明報、am730のネットワークエラー解決
   - その他香港メディアの追加

2. **フィルタリング改善**
   - より高度な香港関連度判定
   - 機械学習ベースの分類

3. **記事品質向上**
   - より自然な日本語翻訳
   - 記事の構造化改善

4. **パフォーマンス最適化**
   - 並列処理の導入
   - キャッシュ機能の追加

